export const colors = {
  BLUE: 'steelBlue',
  GREEN: 'DarkGreen',
  RED: 'Red'
}

export class D3Graph {
  constructor(label, data, color) {
    this.label = label
    this.data = data
    this.color = color
  }
}
